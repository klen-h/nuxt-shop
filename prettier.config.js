export default {
  // 每行最大长度
  printWidth: 80,
  
  // 使用 2 个空格缩进
  tabWidth: 2,
  
  // 使用空格而不是制表符
  useTabs: false,
  
  // 在语句末尾添加分号
  semi: true,
  
  // 使用单引号
  singleQuote: true,
  
  // 对象属性使用引号（仅在必要时）
  quoteProps: 'as-needed',
  
  // 在 JSX 中使用单引号
  jsxSingleQuote: true,
  
  // 在对象字面量的括号前后添加空格
  bracketSpacing: true,
  
  // 将多行 JSX 元素的 > 放在最后一行的末尾
  bracketSameLine: false,
  
  // 箭头函数参数周围使用括号（仅在必要时）
  arrowParens: 'avoid',
  
  // 在文件末尾添加换行符
  endOfLine: 'lf',
  
  // 忽略特定文件
  ignorePath: '.prettierignore',
  
  // Vue 文件中的 script 和 style 标签内容格式化
  vueIndentScriptAndStyle: false
} 